<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todo App</title>
  <style>
    body {
      display: flex;
      align-items: center;
      height: 100vh;
      margin: 0;
      flex-direction: column;
      padding-top: 10px;
      font-family: Arial, sans-serif;
    }

    img {
      width: 500px;
      height: auto;
      border-radius: 8px;
    }

    h1 {
      text-align: center;
    }

    form {
      margin-top: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    input[type="text"] {
      width: 300px;
      padding: 10px;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 16px;
      margin-bottom: 10px;
    }

    input[type="submit"] {
      padding: 10px 20px;
      background-color: #007BFF;
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
    }

    input[type="submit"]:hover {
      background-color: #0056b3;
    }

    .todo-list {
      margin-top: 20px;
      width: 300px;
    }

    .todo-item {
      background-color: #f8f9fa;
      padding: 10px;
      border: 1px solid #ddd;
      border-radius: 8px;
      margin-bottom: 5px;
    }
  </style>
</head>
<body>
  <h1>Todo App</h1>
  <img src="<%= @image_path %>" alt="Random Image" />

  <!-- Input form for new todos -->
  <form action="/todo-app/add_todo" method="POST">
    <input 
      type="text" 
      id="todo-input" 
      name="todo" 
      placeholder="Enter a new todo (max 140 chars)" 
      maxlength="140"
      required
    />
    <input type="submit" value="Add Todo" />
  </form>

  <!-- Hardcoded Todo List -->
  <div class="todo-list">
    <h2>Existing Todos:</h2>
    <ul>
      <% @todos.each do |todo| %>
        <li class="todo-item"><%= todo["task"] %></li>
      <% end %>
    </ul>
  </div>
</body>
</html>
